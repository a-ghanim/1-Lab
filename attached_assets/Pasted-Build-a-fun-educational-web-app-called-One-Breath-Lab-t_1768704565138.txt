Build a fun, educational web app called "One Breath Lab" that lets users type or enter any scientific/philosophical concept (like "black holes", "inflation", "photosynthesis", "attachment theory", "pendulum motion") and instantly generates an interactive p5.js simulation of that concept, plus 3 engaging challenge questions with answers.

Core requirements:
- Use p5.js (include via CDN: https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.9.0/p5.js)
- Clean, modern UI: centered title "One Breath Lab", large input field for the concept, big "Generate Simulation" button, and a responsive canvas area below.
- When user clicks Generate:
  1. Take the user's input concept.
  2. Use AI (you/Replit's built-in models or integrate OpenAI if needed) to generate complete, valid p5.js sketch code that creates an interactive visualization of the concept. Include at least one slider or interactive control (e.g., mass, speed, angle, time) that changes the simulation in real-time.
  3. Safely inject and run that generated p5.js code inside a dedicated div on the page (use new p5() constructor).
  4. Also generate 3 thoughtful challenge questions about the concept (multiple choice or short answer style) with correct answers shown below the canvas.
- Make it robust: If the concept is too vague or impossible, fall back to a nice error message or example simulation.
- Add loading spinner while generating.
- Style everything nicely: dark mode friendly, sans-serif fonts, some subtle animations (fade-in canvas), mobile responsive.
- Deploy-ready: Make sure it runs instantly on Replit's preview.

Start by creating index.html with the basic structure, p5.js CDN, input/button, output divs, and a placeholder canvas. Then implement the generation logic step-by-step.

After building the core, add these nice-to-haves if time:
- A few hardcoded example buttons (e.g., "Try Black Holes", "Try Photosynthesis") that auto-fill and generate.
- Shareable link (update URL with ?concept=... so refreshing keeps the sim).
- Display the generated p5.js code in a collapsible section for transparency/education.

Go step-by-step, use checkpoints after major features, and ask me for clarification only if truly stuck. Build fast and make it feel magical!